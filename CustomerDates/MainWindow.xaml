<Window xmlns:UserControls="clr-namespace:CustomerDates.UserControls" 
        
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="using:Microsoft.UI.Xaml.Controls"
        xmlns:local="clr-namespace:CustomerDates"
        xmlns:MainWindowControls="clr-namespace:CustomerDates.MainWindowControls"
        x:Class="CustomerDates.MainWindow"
        mc:Ignorable="d"
        Height="650" Width="1200" WindowStartupLocation="CenterScreen"
        Closed="Window_Closed" WindowStyle="None" AllowsTransparency="True" AllowDrop="False"
        ResizeMode="NoResize" MinWidth="1200" MinHeight="650" KeyDown="Window_KeyDown" Loaded="Window_Loaded"
        Background="White" Title="Customer Dates">

    <Window.Resources>
        <FontFamily x:Key="cdfont">Arial</FontFamily>

        <Style x:Key="ColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Background" Value="Gainsboro"/>
            <Setter Property="FontFamily" Value="Eagle"/>
            <Setter Property="Padding" Value="3,1,0,0"/>
            <Setter Property="FontFamily" Value="{StaticResource cdfont}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="23"/>
        </Style>

        <Thickness x:Key="Pdng">2,2.5,0,0</Thickness>







        <ControlTemplate x:Key="MenuItemsStandardStyle" TargetType="{x:Type Button}">
            <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsDefaulted" Value="True">
                    <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FF665CFF"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FFFFFFFF"/>
                    <Setter Property="BorderThickness" TargetName="border" Value="2,0,0,0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FF000000"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FFFFFFFF"/>
                </Trigger>
                <Trigger Property="ToggleButton.IsChecked" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FFBCDDEE"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FF245A83"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        



    </Window.Resources>
    <Grid Margin="0">

        <DockPanel x:Name="Toolsdock" Background="#FF050059" Height="33" VerticalAlignment="Top" HorizontalAlignment="Stretch" MouseDown="Toolsdock_MouseDown">
            <Button HorizontalAlignment="Left" x:Name="Menubtn" VerticalAlignment="Top" Width="21" Height="20" Margin="7,7,0,0" BorderBrush="{x:Null}" BorderThickness="0" Cursor="Hand" >
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type ButtonBase}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Button.IsDefaulted" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border" >
                                    <Setter.Value>
                                        <ImageBrush ImageSource="Resources/Assets/Menu blue.png" Stretch="Uniform" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="border">

                                    <Setter.Value>
                                        <ImageBrush ImageSource="Resources/Assets/Menu.png" Stretch="Uniform" Opacity="0.8"/>
                                    </Setter.Value>
                                </Setter>


                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <Button.Background>
                    <ImageBrush ImageSource="Resources/Assets/Menu.png" Stretch="Uniform"/>
                </Button.Background>
            </Button>
            <Separator HorizontalAlignment="Left" Height="4" Margin="0,15,0,0" VerticalAlignment="Top" Width="25" RenderTransformOrigin="0.5,0.5" Background="White">
                <Separator.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-90"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Separator.RenderTransform>
            </Separator>
            <Button  HorizontalAlignment="Right" x:Name="MMbtnexit" Style="{DynamicResource ExitButtonStyle}" VerticalAlignment="Center" Width="15" Height="15" BorderBrush="{x:Null}" Click="MMbtnexit_Click" Cursor="Hand" Margin="0,0,10,0" DockPanel.Dock="Right">
                <Button.Background>
                    <ImageBrush ImageSource="Resources/Assets/Exit Icon (Program).png" Stretch="Uniform"/>
                </Button.Background>
            </Button>
            <Button Width="15" Height="15" x:Name="Maximizebtn" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Click="Maximizebtn_Click" BorderBrush="{x:Null}" Cursor="Hand" DockPanel.Dock="Right" Template="{DynamicResource MaximizeButtonStyle}" >
                <Button.Background>
                    <ImageBrush ImageSource="Resources/Assets/Maximize.png" Stretch="Uniform"/>
                </Button.Background>
            </Button>
            <Button  HorizontalAlignment="Right" Click="Minimizebtn_Click" x:Name="Minimizebtn" Margin="0,0,10,0" VerticalAlignment="Center" Width="15" Height="15" BorderBrush="{x:Null}" BorderThickness="0" Cursor="Hand" DockPanel.Dock="Right" Template="{DynamicResource MinimizeButtonStyle}">
                <Button.Background>
                    <ImageBrush ImageSource="Resources/Assets/Minimize icon.png" Stretch="Uniform"/>
                </Button.Background>
            </Button>
            <UserControls:DeviceTools HorizontalAlignment="Left" Height="33" VerticalAlignment="Top" Margin="0,0,0,-0.2" />
        </DockPanel>


        <Canvas x:Name="Statusbar" Height="20" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Background="#FF050059" Margin="0,0,0,-0.4">
            <Label Margin="0" Padding="0,0,0,0" Height="16" x:Name="Status" Content="Starting..." Width="660" FontSize="14" Foreground="White" FontFamily="{StaticResource cdfont}" Canvas.Top="2" Canvas.Left="2"/>
        </Canvas>

        <MainWindowControls:DockPanelLeft  x:FieldModifier="public" x:Name="DockLeft" HorizontalAlignment="Left" Margin="0,33,0,19.6" VerticalAlignment="Stretch"/>
        <Grid Name="Viewgrid" HorizontalAlignment="Stretch"  Margin="130,33,0,19.6" VerticalAlignment="Stretch" />

    </Grid>
</Window>
    
